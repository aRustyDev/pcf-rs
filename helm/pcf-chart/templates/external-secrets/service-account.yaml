{{- if .Values.external-secrets.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.vaultBackendStore.auth.kubernetes.serviceAccountRef.name }}
  namespace: {{ include "pcf-chart.namespace" . }}
  labels:
    {{- include "pcf-chart.labels" . | nindent 4 }}
  annotations:
    {{- include "pcf-chart.linkerdAnnotations" . | nindent 4 }}
{{- end }}